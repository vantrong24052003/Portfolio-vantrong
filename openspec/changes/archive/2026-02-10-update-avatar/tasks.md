## 1. Phát triển (Implementation)

- [x] 1.1 Cập nhật `AVATAR_URL` trong `IdentityGlitch.tsx` sang `/avatar.jpg`.
- [x] 1.2 Điều chỉnh className của thẻ `img` để hiển thị ảnh rõ ràng hơn (bỏ grayscale).

## 2. Kiểm tra (Verification)

- [x] 2.1 Truy cập trang Connection và xác nhận avatar mới đã được hiển thị.
